---

## description: Generate a deterministic OpenCode execution prompt

Use the Orchestrator Architect skill.

Task:

Generate a deterministic execution prompt for OpenCode.

Target:

$ARGUMENTS

Constraints:

preserve pipeline contracts
avoid redesign
keep scope limited
ensure deterministic output

Output format:

execution objective
target file(s)
required behavior
forbidden behavior
expected result
